apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: omnia-predsednik-2022
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/configuration-snippet: |
      rewrite ^(/zemljevid/omnia)$ $1/ redirect; # add trailing slash
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - vodici.djnd.si
      secretName: omnia-predsednik-2022-tls
  rules:
    - host: vodici.djnd.si
      http:
        paths:
          - path: /zemljevid/omnia(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: omnia-predsednik-2022
                port:
                  number: 80
