apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: omnia-za
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/configuration-snippet: |
      rewrite ^(/zemljevid/omnia/za)$ $1/ redirect; # add trailing slash
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - vodici.djnd.si
      secretName: omnia-za-tls
  rules:
    - host: vodici.djnd.si
      http:
        paths:
          - path: /zemljevid/omnia/za(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: omnia-za
                port:
                  number: 80
