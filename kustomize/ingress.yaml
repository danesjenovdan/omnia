apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: omnia-voda-add-trailing-slash
spec:
  redirectRegex:
    regex: (/zemljevid/omnia/voda)(\?.*)?$
    replacement: ${1}/${2}
    permanent: false
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: omnia-voda-strip-prefix
spec:
  stripPrefix:
    prefixes:
      - /zemljevid/omnia/voda
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: omnia-voda
  annotations:
    # Do not put cert manager here, root redirect ingress (in `ops` repo) will handle it.
    # If we put it here, they will both try to issue the certificate and letsencrypt will rate limit us.
    traefik.ingress.kubernetes.io/router.middlewares: |
      traefik-redirect-to-https@kubernetescrd,
      vodici-redirect-domain@kubernetescrd,
      vodici-omnia-voda-add-trailing-slash@kubernetescrd,
      vodici-omnia-voda-strip-prefix@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: vodici.danesjenovdan.si
      http:
        paths:
          - path: /zemljevid/omnia/voda
            pathType: Prefix
            backend:
              service:
                name: omnia-voda
                port:
                  number: 80
