apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: omnia-za-add-trailing-slash
spec:
  redirectRegex:
    regex: (/zemljevid/omnia/za)(\?.*)?$
    replacement: ${1}/${2}
    permanent: false
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: omnia-za-strip-prefix
spec:
  stripPrefix:
    prefixes:
      - /zemljevid/omnia/za
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: omnia-za
  annotations:
    # Do not put cert manager here, root redirect ingress (in `ops` repo) will handle it.
    # If we put it here, they will both try to issue the certificate and letsencrypt will rate limit us.
    traefik.ingress.kubernetes.io/router.middlewares: |
      traefik-redirect-to-https@kubernetescrd,
      vodici-redirect-domain@kubernetescrd,
      vodici-omnia-za-add-trailing-slash@kubernetescrd,
      vodici-omnia-za-strip-prefix@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: vodici.danesjenovdan.si
      http:
        paths:
          - path: /zemljevid/omnia/za
            pathType: Prefix
            backend:
              service:
                name: omnia-za
                port:
                  number: 80
