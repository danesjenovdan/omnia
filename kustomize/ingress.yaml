apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: omnia-voda
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/configuration-snippet: |
      rewrite ^(/zemljevid/omnia/voda)$ $1/ redirect; # add trailing slash
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - vodici.djnd.si
        - vodici.danesjenovdan.si
      secretName: omnia-voda-tls
  rules:
    - host: vodici.djnd.si
      http:
        paths:
          - path: /zemljevid/omnia/voda(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: omnia-voda
                port:
                  number: 80
    - host: vodici.danesjenovdan.si
      http:
        paths:
          - path: /zemljevid/omnia/voda(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: omnia-voda
                port:
                  number: 80
